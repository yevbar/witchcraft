"""forge_integration — drive games inside Forge (the authoritative Java engine) via our Python policy.

forge_bridge: our agent plays AS A PLAYER inside a Forge match (Forge owns the rules). The run_*.py scripts
stand up the JVM connector; the Forge*.java files are the Java side.
"""

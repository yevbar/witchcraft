// 3-rule transitive-closure toy: the canonical incremental-Datalog sanity program.
.decl edge(x:number, y:number)
.input edge
.decl path(x:number, y:number)
.output path
path(x, y) :- edge(x, y).
path(x, y) :- edge(x, z), path(z, y).

# MTGJSON card database (525MB) — downloaded separately, used only by bridge_cards.py
AllPrintings.json
# derived, unreferenced card export
cards.jsonl

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Soufflé / scratch output
*.csv
/tmp/
.claude/

# MTGJSON bulk data (large; fetch via mtgjson.com)
# match both the directory AND a bare `mtgjson` symlink/file — a self-referential
# symlink here once got committed because `mtgjson/` only matches directories.
mtgjson
mtgjson/
datalog/cards.dl

# ReBeL training output artifacts
rebel_runs/

# Locally-built / shipped engine binaries (the future per-arch wheel ships these; see mtg/_native.py)
packages/mtg/_native/

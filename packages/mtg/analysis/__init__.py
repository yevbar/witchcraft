"""mtg.analysis — card/deck analysis libraries derived from the engine's structured facts.

deck_evaluator (how a deck wins), interaction_evaluator (pairwise synergy graph), card_synergy (static
buffs graph), card_spacy (bridge the card interpreter to the rules engine). Derived from KNOWN mechanics,
not oracle-text guessing.
"""
